<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp"
    tools:context="taller2.match_client.MainActivity"
    tools:showIn="@layout/activity_main">

    <EditText
        android:id="@+id/userMailLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/text_design_login"
        android:inputType="textPersonName"
        android:hint="@string/prompt_user_mail_login"
        android:textColor="#706d6d"
        android:textCursorDrawable="@null"
        android:layout_above="@+id/userPasswordLogin"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"/>

    <EditText
        android:id="@+id/userPasswordLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/text_design_login"
        android:inputType="textPassword"
        android:hint="@string/prompt_user_password_login"
        android:textColor="#706d6d"
        android:textCursorDrawable="@null"
        android:layout_above="@+id/loginButton"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp" />

    <Button
        android:id="@+id/loginButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/login_button"
        android:text="Login"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="7dp"
        android:layout_marginBottom="10dp"
        android:layout_above="@+id/registerButton" />

    <Button
        android:id="@+id/registerButton"
        android:layout_width="wrap_content"
        android:layout_height="42dp"
        android:text="Register Now"
        android:textColor="#267055"
        android:background="@drawable/register_button"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:paddingLeft="16dp"
        android:paddingRight="16dp" />

</RelativeLayout>
